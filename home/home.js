// Home page JS — Webflow Home Page Settings → Footer Code
(function(){
  if (window.VshHome) return;
  window.VshHome = { ready: false };

  // Home page logic: loader, scrub mode, job item trail, typed.js, lenis, etc.
  // Stories functionality lives in vshslv-stories repo, loaded separately.

  window.VshHome.ready = true;
})();
